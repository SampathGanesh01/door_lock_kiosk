# ─────────────────────────────────────────────────────────────────
#  Door Lock Kiosk — Python Dependencies
#  Install with:  pip install -r requirements.txt
#
#  Ubuntu system packages needed before pip install:
#    sudo apt update
#    sudo apt install -y python3-pip python3-venv \
#         libgl1 libglib2.0-0 libsm6 libxrender1 libxext6 \
#         python3-pyside6.qtcore python3-pyside6.qtgui python3-pyside6.qtwidgets
# ─────────────────────────────────────────────────────────────────

# Computer Vision — camera capture, Haar face detection, drawing
# Ubuntu/Linux kiosk with display window → use opencv-python (NOT headless)
opencv-python>=4.8.0

# Numerical computing — ArcFace embedding vectors, cosine distance
numpy>=1.24.0

# Deep-learning face recognition — ArcFace 512-D embeddings
#   First run downloads ~500 MB of model weights (cached in ~/.deepface/)
deepface>=0.0.93

# TensorFlow backend required by DeepFace/ArcFace
# macOS (Intel):        tensorflow>=2.13.0
# macOS Apple Silicon:  tensorflow-macos>=2.13.0  (uncomment below)
# Ubuntu/Linux x86_64:  tensorflow>=2.13.0
tensorflow>=2.13.0
# tensorflow-macos>=2.13.0

# Arduino / serial door control — for --port flag
#   Only needed if an Arduino is wired up
pyserial>=3.5

# PySide6 — full-screen kiosk UI (frameless window, no taskbar)
# On Ubuntu, prefer the pip package:
PySide6>=6.5.0
requests
